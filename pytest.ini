# content of pytest.ini

[pytest]
markers =
    ci
    linux
    slow

# Ensure loop detected
timeout = 60
timeout_method = thread

filterwarnings =
    ignore::DeprecationWarning:aenum.*:
    ignore::DeprecationWarning:hyper.*:
    ignore::DeprecationWarning:nose.*:
